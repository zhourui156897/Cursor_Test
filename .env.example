# ============================================
# 第二大脑 (dierdanao) - 环境配置
# 复制此文件为 .env 并根据你的环境修改
# cp .env.example .env
# ============================================

# --- 认证模式 ---
# "single" = 个人模式（无需登录，所有操作视为admin）
# "multi"  = 团队模式（JWT登录，RBAC权限控制）
AUTH_MODE=single
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme
JWT_SECRET=please-change-this-to-a-random-string

# --- 路径 ---
OBSIDIAN_VAULT_PATH=~/Documents/ObsidianVault
DATA_DIR=~/.dierdanao/data

# --- 服务端口 ---
BACKEND_PORT=8000
FRONTEND_PORT=3000
MILVUS_PORT=19530
NEO4J_HTTP_PORT=7474
NEO4J_BOLT_PORT=7687

# --- 向量数据库模式 ---
# "milvus"      = 完整Milvus Standalone（需Docker，推荐16GB+内存）
# "milvus-lite"  = 嵌入式Milvus Lite（无需Docker，适合8GB内存测试）
VECTOR_DB_MODE=milvus-lite

# --- LLM ---
LLM_API_URL=http://localhost:11434/v1
LLM_MODEL=qwen2.5
EMBEDDING_MODEL=bge-m3
EMBEDDING_DIM=1024

# --- Neo4j ---
NEO4J_USER=neo4j
NEO4J_PASSWORD=dierdanao123
NEO4J_URI=bolt://localhost:7687
