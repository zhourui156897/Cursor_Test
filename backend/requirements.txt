# 第二大脑 后端依赖（版本锁定，便于另一台 Mac 复现安装）
# 生成方式：在 backend 目录下 venv 中 pip install -e . 后执行 pip freeze --exclude-editable
# 依赖变更后请同步更新此文件

fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
aiosqlite>=0.20.0
python-jose[cryptography]>=3.3.0
passlib>=1.7.4
bcrypt>=4.1,<5
python-multipart>=0.0.9
watchfiles>=0.21.0
httpx>=0.27.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
pymilvus>=2.4.0
neo4j>=5.0.0
apscheduler>=3.10.0
